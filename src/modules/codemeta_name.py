def get(repository_url: str) -> dict:
    """Extracts the software name from the GitHub repository."""
    # In a real implementation, this would fetch the name from the URL
    return {"name": "sshoc-nl-codemeta-generator"}
